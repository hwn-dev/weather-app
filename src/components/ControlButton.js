import { render } from '@testing-library/react';
import React from 'react';

const ControlButton = () => {
    return (
        <button>
            Control Button
         </button>
        )
}

export default ControlButton;